{
  "installCommand": "npm ci --prefer-offline --no-audit --progress=false",
  "buildCommand": "node scripts/build-projects.js",
  "engines": {
    "node": "18.x"
  },
  "build": {
    "env": {
      "CI": "false"
    }
  }
}
